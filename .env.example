# ============================================
# ВАЖНО: Этот файл - только шаблон!
# НЕ используйте эти пароли в production!
# ============================================

# PostgreSQL Configuration
POSTGRES_DB=tasksdb
POSTGRES_USER=taskuser
POSTGRES_PASSWORD=dbpass
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Flask Configuration
FLASK_ENV=production
FLASK_PORT=5041

# Borg Backup Configuration
# ВАЖНО: Используйте сильный пароль минимум 16 символов!
BORG_PASSPHRASE=borgpass
BORG_REPO=/home/app/Work-project/backups/borg-repo

# Container Names
CONTAINER_DB=taskzilla-db
CONTAINER_WEB=taskzilla-web

# ============================================
# Инструкция по настройке:
# 1. Скопируйте этот файл: cp .env.example .env
# 2. Отредактируйте .env и замените ВСЕ пароли
# 3. Установите безопасные права: chmod 600 .env
# 4. Проверьте конфигурацию: make check-env
# ============================================
